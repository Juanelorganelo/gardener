{
  "oneOf": [
    {
      "type": "object",
      "properties": {
        "pattern": {
          "type": "string",
          "format": "regexp",
          "description": "The pattern used to validate branch names"
        },
        "exclude": {
          "type": "array",
          "items": {
            "type": "string"
          }
        }
      },
      "required": ["pattern"]
    },
    {
      "type": "object",
      "properties": {
        "preset": {
          "type": "string",
          "enum": "git-flow",
          "description": "Use on of the pre-configured patterns to validate branch names"
        },
        "exclude": {
          "type": "array",
          "items": {
            "type": "string"
          }
        }
      }
    }
  ]
}
